const JSON = {
    description: 'JSON Files',
    accept: {
        'application/json': ['.json'],
    },
}

const TEXT = {
    description: 'Text Files',
    accept: {
        'text/plain': ['.txt'],
    },
}

export const FILE_TYPES = [
    JSON,
    TEXT,
]
